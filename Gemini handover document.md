작업 인수인계 (AI A → AI B)1. 목표2번 '일별 기록 & 어제 대비 변화 재계산' 패널에 사용자가 기존의 모든 일별 기록을 삭제할 수 있는 '전체 기록 삭제' 버튼 기능 추가.2. 현재 상태UI 하단에 붉은색 '전체 기록 삭제' 버튼 렌더링 완료.버튼 클릭 시 사용자 의사를 묻는 confirm 창 출력까지 구현됨.삭제 처리를 위한 clearAllBtn의 EventListener가 renderExportButton 함수 안에 작성되어 있음.3. 실행 명령브라우저에서 index.html을 엽니다.1번 섹션에서 '지금 조회하기'를 눌러 데이터를 1개 이상 쌓습니다.2번 섹션 테이블 하단에 붉은색 '전체 기록 삭제' 버튼이 노출되는 것을 확인합니다.버튼을 누르고 알림창에서 '확인'을 누릅니다.4. 통과 검사 (TEST-01 ~ TEST-09)TEST-01 ~ 06: 기존 핵심 기능(조회, 에러 처리, 스캔) 모두 정상 동작.TEST-07: 기록 존재 시 버튼 노출 성공.TEST-08: 버튼 클릭 시 confirm 알림창 호출 성공.TEST-09: 알림창 '취소' 시 데이터 유지 성공.5. 남은 문제 (TEST-10 실패)[ID: TEST-10] 확인 창에서 '확인' 클릭 시 데이터가 지워져야 하지만, 삭제 로직 내부에서 오류가 발생해 테이블이 갱신되지 않고 콘솔과 알림으로 에러가 뜹니다.원인: renderExportButton 내 클릭 이벤트 리스너에서 순회 삭제 시 appStorage.remove(k)를 호출하도록 작성했으나, appStorage 객체의 실제 삭제 메서드는 remove가 아니라 delete입니다.6. 다음 행동후임 작업자는 index.html 내 renderExportButton 함수의 삭제 로직에서 발생한 메서드 호출 오류(appStorage.remove(k))를 찾아 올바른 메서드(appStorage.delete(k))로 수정하여 TEST-10을 통과시키십시오.7. 건드리지 말 것appStorage 인터페이스 자체의 구조 및 기존 1번, 3번, 4번 패널의 기능과 HTML 구조는 절대 수정하지 마십시오.10개의 고정 검사 기준을 수정하거나 완화하지 마십시오.